const module = 'App';

export const TOGGLE_MODAL = `${module}/TOGGLE_MODAL`;
export const CHANGE_PARAM = `${module}/CHANGE_PARAM`;

export const LOGIN_REQUEST = `${module}/LOGIN_REQUEST`;
export const LOGIN_SUCCESS = `${module}/LOGIN_SUCCESS`;
export const LOGIN_FAILURE = `${module}/LOGIN_FAILURE`;

export default module;
